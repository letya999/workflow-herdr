## AI Repo Safety Addendum

Run `ai-repo-safety scan --target .` before commits and `ai-repo-safety prepush --target .` before pushes. Use `ai-repo-safety github-guard` or `ai-repo-safety gitlab-guard` for reading hosted forge issues, merge requests, pull requests, commits, and branches into AI context.
